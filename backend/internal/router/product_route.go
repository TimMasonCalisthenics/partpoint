package router

import (
	// "backend/internal/product"

	"github.com/gin-gonic/gin"
)

func SetupProductRoutes(r *gin.Engine) {
	// r.GET("/products", product.GetProducts)
	// r.GET("/products/:id", product.GetProductDetail)
}